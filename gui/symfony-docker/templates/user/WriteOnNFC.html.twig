{% extends 'template.html.twig' %}

{% block body %}

<div class="container-fluid">
    <h1>Présenter un tag NFC à proximité du scanner, puis cliquer sur le bouton "Ecrire" </h1><br>
    <form>
        <button class="btn btn-primary" type="button" data-toggle="modal" data-target="#nfcModal">Ecrire</button>
        <input type="hidden" name="tokenID" value="{{id}}">
    </form>
</div>

<!-- Modal -->
<div class="modal fade" id="nfcModal" tabindex="-1" aria-labelledby="nfcModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="nfcModalLabel">NFC Write Status</h5>
        <button type="submit" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        The NFC tag write process has started. Please wait...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

{% endblock %}

{#}
{% extends 'template.html.twig' %}

{% block body %}

<div class="container-fluid">
    <h1>Présenter un tag NFC à proximité du scanner, puis cliquer sur le bouton "Ecrire" </h1><br>
        <form action="{{ path('app_nfc_write', {'id': id}) }}" method="post">
        <button class="btn btn-primary" type="submit" >Ecrire</button>
        <input type="hidden" name="tokenID" value="{{id}}">
        </form>
        {# <h3>Entrez le tag NFC</h3>
        <input type="text" name="tag" class="form-control" placeholder="{{id}}" required> #}


    {# </div> #}

{# {% endblock %} #}
