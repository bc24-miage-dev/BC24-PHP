{# templates/admin/create_production_site.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Production Site</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Create</button>
    {{ form_end(form) }}

{% endblock %}