{% extends 'pro/eleveur/index.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <h1>Enregistrez votre source de Nutrition :</h1>
        {{ form_start(form) }}
            <label> Nutrition :
                {{ form_widget(form.nutrition, {'attr': {'class': 'form-control'}}) }}
            </label>
        {{ form_end(form) }}
        <br>
        <a href="{{ path('app_eleveur_specific', {'id' : id}) }}"><button class="btn btn-primary">Revenir en arrière</button></a>
    </div>
{% endblock %}
